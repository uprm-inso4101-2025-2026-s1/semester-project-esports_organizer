{
  "name": "database-emulator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "emu": "firebase emulators:start --only firestore --project local-esports-organizer",
    "emu:health": "node emulator-health.js"
  },
  "devDependencies": {
    "firebase-tools": "^13.35.1"
  },
  "dependencies": {
	"firebase": "^12.3.0"
  }
}
