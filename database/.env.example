# Example environment variables for local Firestore emulator
# Copy the VITE_* variables into .github/esports-organizer/.env.local to have the Vite app auto-connect to the emulator.

# Emulator/CLI settings (used for docs/reference)
FIREBASE_PROJECT_ID=local-esports-organizer
FIRESTORE_EMULATOR_HOST=127.0.0.1
FIRESTORE_EMULATOR_PORT=8080
FIREBASE_EMULATOR_UI_PORT=4000

# Frontend (Vite) settings — put these in .github/esports-organizer/.env.local
VITE_USE_EMULATORS=true
VITE_FIREBASE_PROJECT_ID=local-esports-organizer
VITE_FIRESTORE_EMULATOR_HOST=127.0.0.1
VITE_FIRESTORE_EMULATOR_PORT=8080

